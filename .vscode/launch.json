{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Session View Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": ["${workspaceFolder}", "--extensionDevelopmentPath=${workspaceFolder}/extension"]
    }
  ]
}
