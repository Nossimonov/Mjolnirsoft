/**
 * Placeholder entry point for the orchestration tooling.
 * Real modules land here as features are built; the export below exists only
 * to give the test harness something to exercise.
 */
export function sum(a: number, b: number): number {
  return a + b;
}
